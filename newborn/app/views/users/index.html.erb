<h1>Listing users</h1>
 
<table>
  <tr>
    <th>Name</th>
  </tr>
 
  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= link_to 'Show', user_path(user) %></td>
    </tr>
  <% end %>
</table>

<p>
  <%= link_to 'New User', new_user_path %>
</p>